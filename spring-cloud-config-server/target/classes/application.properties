spring.application.name=spring-cloud-config-server
server.port=8888

# Use classpath (native) instead of a Git repo
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=classpath:/config
